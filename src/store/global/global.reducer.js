import * as types from './../types';

const initialState = {};

export const globalReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}